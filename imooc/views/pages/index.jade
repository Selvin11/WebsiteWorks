extends ../layout

block content
	.container
		.row
		each item in movies
				.col-md-2
					a(href="/movie/#{item_id")
						img(src="#{item.poster}",alt="#{item.title}")
						h3 #{item.title}
						p: a.btn.btn-primary(href="/movie/#{item_id}",role="button")
							观看预告片

	